import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  buttons: {
    alignItems:"center",
    marginHorizontal: 30,
  },
  
});

export default styles;
